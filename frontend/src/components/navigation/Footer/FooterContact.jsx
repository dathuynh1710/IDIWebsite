/**
 * FooterContact — Navigation: FooterContact
 * TODO: Implement component
 */
export default function FooterContact({ className, children, ...props }) {
  return (
    <div className={className} {...props}>
      {children}
    </div>
  )
}
