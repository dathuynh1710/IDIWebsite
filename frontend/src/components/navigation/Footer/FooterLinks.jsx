/**
 * FooterLinks — Navigation: FooterLinks
 * TODO: Implement component
 */
export default function FooterLinks({ className, children, ...props }) {
  return (
    <div className={className} {...props}>
      {children}
    </div>
  )
}
