/**
 * CertificationsBar — Section: CertificationsBar
 * TODO: Implement component
 */
export default function CertificationsBar({ className, children, ...props }) {
  return (
    <div className={className} {...props}>
      {children}
    </div>
  )
}
