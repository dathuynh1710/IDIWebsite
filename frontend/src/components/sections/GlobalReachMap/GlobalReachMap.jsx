/**
 * GlobalReachMap — Section: GlobalReachMap
 * TODO: Implement component
 */
export default function GlobalReachMap({ className, children, ...props }) {
  return (
    <div className={className} {...props}>
      {children}
    </div>
  )
}
