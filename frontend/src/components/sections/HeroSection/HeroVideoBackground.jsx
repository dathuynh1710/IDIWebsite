/**
 * HeroVideoBackground — Section: HeroVideoBackground
 * TODO: Implement component
 */
export default function HeroVideoBackground({ className, children, ...props }) {
  return (
    <div className={className} {...props}>
      {children}
    </div>
  )
}
