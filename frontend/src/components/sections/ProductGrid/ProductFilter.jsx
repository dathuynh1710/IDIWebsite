/**
 * ProductFilter — Section: ProductFilter
 * TODO: Implement component
 */
export default function ProductFilter({ className, children, ...props }) {
  return (
    <div className={className} {...props}>
      {children}
    </div>
  )
}
