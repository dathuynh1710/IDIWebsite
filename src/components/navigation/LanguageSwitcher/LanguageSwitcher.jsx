/**
 * LanguageSwitcher — Navigation: LanguageSwitcher
 * TODO: Implement component
 */
export default function LanguageSwitcher({ className, children, ...props }) {
  return (
    <div className={className} {...props}>
      {children}
    </div>
  )
}
