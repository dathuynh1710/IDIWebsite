/**
 * PartnersCarousel — Section: PartnersCarousel
 * TODO: Implement component
 */
export default function PartnersCarousel({ className, children, ...props }) {
  return (
    <div className={className} {...props}>
      {children}
    </div>
  )
}
