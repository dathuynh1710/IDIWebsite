/**
 * SustainabilityBanner — Section: SustainabilityBanner
 * TODO: Implement component
 */
export default function SustainabilityBanner({ className, children, ...props }) {
  return (
    <div className={className} {...props}>
      {children}
    </div>
  )
}
